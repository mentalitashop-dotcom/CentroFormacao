import { GestorResumoComponent } from './gestor-resumo.component';

describe('GestorResumoComponent', () => {
  it('should be defined', () => {
    expect(GestorResumoComponent).toBeDefined();
  });
});
