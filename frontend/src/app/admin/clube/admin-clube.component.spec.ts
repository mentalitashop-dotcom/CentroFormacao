import { AdminClubeComponent } from './admin-clube.component';

describe('AdminClubeComponent', () => {
  it('should be defined', () => {
    expect(AdminClubeComponent).toBeDefined();
  });
});
